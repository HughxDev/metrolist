/* eslint-disable no-script-url */
import React from 'react';

import Card from '../index';
// import CardTopicText from './index';

export default {
  "title": "Card/CardTopicText",
  "component": Card.TopicText,
};

export const Default = () => <Card.TopicText>Topic:</Card.TopicText>;
