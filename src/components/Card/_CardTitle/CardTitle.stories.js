/* eslint-disable no-script-url */
import React from 'react';
import CardTitle from './index';

export default {
  "title": "Card/CardTitle",
  "component": CardTitle,
};

export const Default = () => <CardTitle>Card Title</CardTitle>;
